#!/bin/sh

# alias some commands
alias ll='ls -la'
alias ls='ls -FH'
alias dir='dir -FHG'
alias cp='cp -iv'
alias rm='rm -i'
alias mv='mv -iv'
alias gst='git status'
alias gbr='git branch -vv'
alias mvni='mvn clean install -U -DskipTests -DskipStaticCodeAnalysis'
